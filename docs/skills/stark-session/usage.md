# stark-session

Session management — start and end modes. Start: loads context, git state, health checks, briefing. End: runs tests, merges PRs, commits docs, pushes. Config via .code-review/config.json hierarchy. Use when the user says "session start", "session end", "start session", "end session", "what was I working on", "catch me up", or invokes /stark-session.

## Workflow Overview

```mermaid

```

![A polished one-page infographic for the `stark-session` skill with a top hero showing invocation examples, two side-by-side mode cards for Start and End, blue vertical flow diagrams for each mode with green config nodes, purple decision prompts, red warning states, amber outputs, and gray external dependencies, followed by config tables, prerequisite notes, and common workflow cards for catch-up and session closeout."}}](usage.png)

## When to Use

Session management — start and end modes. Start: loads context, git state, health checks, briefing. End: runs tests, merges PRs, commits docs, pushes. Config via .code-review/config.json hierarchy. Use when the user says "session start", "session end", "start session", "end session", "what was I working on", "catch me up", or invokes /stark-session.

## Prerequisites

*See SKILL.md*

## Arguments

`[start|end]`



## Quick Start

/stark-session

## Common Patterns



## Troubleshooting



## Related Skills


