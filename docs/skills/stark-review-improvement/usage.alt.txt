Alt text: Usage guide for the stark-review-improvement skill showing a 5-phase vertical workflow: (1) Extract Assessment from conversation context or history files, (2) Confirm Action Items with the user via an interactive numbered list categorized as prompt-edit, orchestrator-edit, config-edit, or no-action, (3) Apply Changes as minimal surgical edits to prompt markdown files, multi_review.py orchestrator, and config JSON, (4) Validate with syntax checks for markdown, Python compilation, and JSON parsing followed by a git diff review, and (5) Log and Commit by appending to the prompt changelog and creating a git commit without auto-push. The page includes invocation examples showing /stark-review-improvement and natural language triggers, a prerequisites section requiring a completed /stark-review assessment and the stark-skills repo, an inputs/outputs table, common workflow patterns for immediate, deferred, and selective application, an edit categories reference table, and key constraints emphasizing never rewriting entire files, never removing existing instructions, and maintaining backward compatibility.