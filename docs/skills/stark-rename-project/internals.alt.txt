Internal architecture diagram for the stark-rename-project skill showing an eight-phase pipeline from validated rename inputs through GitHub rename, local directory move, symlink cleanup, project and sibling-repo reference rewrites, reinstall, verification, and summary. Side annotations call out resume-state detection, repo-ID based idempotency, literal-safe replacement rules, dirty-repo skipping, and failure branches for permission, collision, syntax, and install errors.
