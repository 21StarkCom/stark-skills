# CLAUDE.md — stark-skills

## What This Is

The stark skills + tools fleet: a **two-stage development pipeline** (`/stark-author` human-gated spec+plan → `/stark-build` check-gated implementation — no LLM-reviews-LLM loops, per the 2026-07-25 autopsy and the 2026-07-26 demolition), a **single-agent PR code reviewer** (`/stark-review`, evidence-contract prompts), multi-agent IaC review, and the ops/session tier. Claude, Codex, and Gemini are all enabled (Gemini → `gemini-3.1-pro-preview`, default auth **oauth** — the logged-in Google account's Code Assist seat; `vertex` and `api-key` modes remain via `models.gemini.auth` / `STARK_GEMINI_AUTH`, see `tools/gemini_auth_lib.ts`). The Vertex **project/location are resolved at runtime** by `tools/vertex_config_lib.ts` (env > config > `GOOGLE_CLOUD_PROJECT` > local `gcloud`) — **never hardcoded/committed in source**; in oauth mode the resolved project doubles as the Code Assist licensing project. Note: `-latest` aliases like `gemini-pro-latest` only resolve via the Generative-Language API-key fallback, **not** Vertex. Hierarchical config (global → org → repo). Self-improving prompts via review history analysis.

## Operating Principles

This is a **personal playground**, not production. No customers depend on it; the only user is the author.

- **No rollout ceremony.** Skip soaking, gating, smoking, canary, and gradual-rollout patterns. Merge straight to main once the PR is green.
- **Branch + PR for everything — nothing lost.** Every change lands on a branch and merges via PR; never commit or push to `main` directly. "Ship straight to main" means merge once green, not bypass the PR. This repo **is** the review system — so every review's findings get posted on the PR as comments (inline where anchored, summary otherwise; `stark_review.ts` already does the GitHub posting). Don't drop, downgrade, or summarize findings away, and don't merge with open findings unaddressed — fix them, or reply on the thread saying why not.
- **Every PR action is `aryeh-stark`; bots only post reviews.** Opening a PR, commenting as Aryeh, resolving a thread, un-drafting, merging, opening an issue — all go through the `gh` CLI, which is logged in as **`aryeh-stark`**. The `stark-{claude,codex,gemini}` GitHub Apps exist for **one** reason: posting multi-LLM review findings, where three distinct bot authors are what makes "which model said this" readable. `tools/github_app{,_lib}.ts` therefore exposes reads plus `pr review`/`pr comment` and **nothing that authors on Aryeh's behalf** — `pr create`, `pr ready`, `pr merge` and `issue create` were removed 2026-08-04 and now exit 2 naming the `gh` command to run. CI is the one exception, since a workflow has no human token (and Aryeh's PAT in Actions secrets would be worse): `marketplace-sync.yml` mints an app token and calls `gh pr create` with it.
  - **The one human-invoked swap: `/stark-gh-user`.** When `aryeh-stark`'s GraphQL/REST bucket runs dry, Aryeh can move `gh` to a relief account for the rate-limited command. It is his call, never an agent's — `disable-model-invocation: true`, and no tool, skill or hook may call `tools/user_token.ts`. Wiring it into an automated path would re-author whatever ran next, which is exactly the failure this rule exists to prevent. Default stays `primary` = `aryeh-stark`; revert with `unset GH_TOKEN GITHUB_TOKEN STARK_GH_USER` when the window closes.
- **Draft PRs by default — test locally, un-draft to merge.** Every PR-opening path opens a **draft** so WIP stays out of draft-guarded CI while you verify locally. The default is owned by the `gh` path — `plugins/stark-gh/tools/gh_pr_open_execute.ts` resolves it through the shared draft config so `pr-open` and `pr-merge` agree on one answer; the review/author/build skills and the automation-fleet prompts pass `--draft` explicitly. Opt out per-run with `--ready` (alias `--no-draft`). **You cannot merge a draft**, so the merge paths un-draft first: `/stark-gh:pr-merge` (`gh pr ready`) marks the PR ready-for-review — which fires the target CI via the `ready_for_review` event — then wait for green, then squash-merge. For "no CI on WIP" to actually hold, each target repo's `pull_request` workflows need the **skip-draft guard** (`if: github.event.pull_request.draft == false` + `ready_for_review` in `types`) — pattern + `== false` vs `!= true` guidance in `standards/workflows/skip-draft-guard.md`. Exempt: push-triggered publishers like `marketplace-sync` that must run CI + auto-merge (they open ready).
- **Language preference:** Go for backend, TypeScript for scripts. **Avoid Python at all costs** — the repo's tooling is now TypeScript-only (`tools/`); the former Python orchestrators + dispatch infra under `scripts/` were migrated out. Do not introduce new Python.
- **Test live.** Local-only verification is not enough. If a flow touches GCP, exercise the real GCP surface.
- **Always update documentation.** Any change that affects behavior, structure, commands, env vars, or operations must update the relevant docs (this file and `AGENTS.md` included) in the same change.

## Distribution

This repo is the **source of truth** for the skills + tools. Distribution is **marketplace-only** — the former `install.sh` symlink installer was removed (2026-07-07); there is no local symlink install step.

- **bifrost** (the marketplace; formerly `stark-marketplace`, renamed 2026-07-11) — the `bifrost` repo packages these skills as separate self-contained Claude Code and native Codex plugins. Claude installs with `/plugin marketplace add 21StarkCom/bifrost` then `/plugin install <bundle>@bifrost`; Codex installs with `codex plugin marketplace add 21StarkCom/bifrost` then `codex plugin add <bundle>@bifrost`. Canonical `skill/`, `plugins/stark-gh/commands/`, and shared assets remain Claude-authored. Complete Codex variants live only under `runtime-overrides/codex/`, which Bifrost imports into a separate `dist/codex-plugins/` package surface; Codex overlays must never enter `dist/claude/`. CI auto-publishes changes to either source surface.
- **Local dev is no longer live.** Editing a file in this repo does not take effect until the change is published (merge to `main` → `marketplace-sync` PR → merge) and the plugin is reinstalled/updated (`/plugin update …`). To test an in-progress edit against a real install, run `stark sync` in the marketplace repo and `/plugin update` locally.
- **Assets plugins don't cover** (user `~/.claude/settings.json`, statusline, output-styles, `org/evinced` overrides, mutable-state dirs, git hooks) are now managed by hand — the installer no longer provisions them.

**The plugin-resolution seam:** `tools/asset_root_lib.ts` — `assetRoot()` resolves immutable assets (tools/prompts/config) from `${CLAUDE_PLUGIN_ROOT}` when running inside an installed plugin, falling back to `~/.claude/code-review` for direct (non-plugin) invocations such as automation-fleet crons; `stateRoot()` always stays under `$HOME`. Skills mirror this with `${CLAUDE_PLUGIN_ROOT:-$HOME/.claude/code-review}`. **Never hardcode `~/.claude/code-review/{tools,prompts,scripts,standards}` in a skill or tool** — route immutable-asset reads through `asset_root_lib`/the dual-fallback so plugin installs keep working.

## Repo Layout

- `global/` — global config + prompts, vendored into each marketplace plugin
- `scripts/` — shell helpers + JSON (`register_triggers.sh`, `healer_patterns.json`), vendored into plugins. The orchestrator + dispatch infra were migrated to `tools/` (TypeScript) — see the Python→TS migration spec.
- `skill/` — all skills (`skill/stark-*/SKILL.md`, 28 skills), packaged as marketplace plugins
- `org/evinced/` — Evinced org config overrides
- `data/` — persona roster, review coverage HTML, generated showcase pages
- `automation/` — CCR automation fleet: 12 triggers, prompts, logs, cost tracking, reports
- `.github/workflows/` — GitHub Actions: project sync, gate checks, stale detection, heartbeat, `marketplace-sync` (auto-publish to bifrost)
- `docs/` — specs, ADRs, retrospectives, generated skill docs (`docs/plans/` is a frozen archive — see Conventions)
- `standards/` — org-wide doc templates and workflows
- `plugins/stark-gh/` — local plugin source, packaged by the marketplace

## Key Files

### Dispatchers & orchestration
- `tools/copilot_land_lib.ts` (pure) + `tools/copilot_land.ts` (CLI) — `/stark-copilot`'s create-or-adopt **impl-PR landing** helper (`branch-name`/`prepare-branch`/`land`): deterministic `copilot/<slug>` branch, push never `--force`, adopt-or-open PR by head ref (draft by default, `--ready` un-drafts even an adopted draft via `gh pr ready`), multi-PR union. **`prepare-branch --require-base <sha>`** (added 2026-07-30) closes a silently-wrong-run class: with no local branch but an existing remote one, the adopt path runs `git checkout -B <branch> origin/<branch>`, which **resets HEAD onto the remote branch** — so a leftover branch from an abandoned run replaces the caller's pinned base with an older codebase while the tool still reports `ok: true`. Reproduced live on a `/stark-build` run (every gate would have measured a tree predating the re-pin). The flag refuses that adoption **before** the destructive checkout on **both** the `checkout-track` and `checkout-ff` paths (the worktree never moves — a stale *local* branch is as dangerous as a stale remote one) and re-asserts `HEAD` contains the base afterwards. Hardened 2026-07-30 after review: an **empty** value is rejected rather than silently disabling every guard; **unknown flags are a hard error** (`--requirebase` used to parse to a no-op and exit 0, so a typo disabled the guard invisibly); `--dry-run` evaluates the guard and reports `would_refuse` instead of green-lighting the case the real run refuses; and containment is **tri-state** — `merge-base --is-ancestor` exit 128 means *unresolvable* (a `--single-branch`/`--depth` clone never created `refs/remotes/origin/<branch>`), NOT stale, because collapsing the two told operators to delete healthy branches holding pushed work. **Both `/stark-build` Phase 1 and `/stark-copilot` §1.7 pass it** — copilot pins the current `HEAD` and its blast radius is larger, since it adopts into `$REPO_ROOT` rather than a throwaway worktree. Phase 1 additionally checks `prepare-branch`'s **exit code** and that HEAD is on the expected branch: a refusal leaves HEAD untouched, so a bare `merge-base --is-ancestor` compares the base to itself and passes.
- `tools/plan_review_dispatch.ts` + `plan_review_dispatch_lib.ts` — plan/spec document review dispatch (N agents × M domains, bounded 21-worker pool)
- `tools/iac_review.ts` (CLI) + `iac_review_lib.ts` (orchestrator) — **multi-agent IaC reviewer** backing `/stark-terraform-review` + `/stark-terragrunt-review`. Resolves a configurable agent list (CLI `--agents` > config `iac_review.agents` > `["codex"]`), collects in-scope `.tf`/`.hcl` (path or `--changed`), runs read-only host scanners as evidence, then dispatches **every agent in parallel as its own headless subagent** with the canonical rubric (`global/prompts/iac-review/{terraform,terragrunt}.md`) + line-numbered context. Parses each agent's JSON findings, dedups across agents (populates `cross_validated_by`), applies `--min-severity`, renders markdown (or `--json`), optionally posts to a PR via `github_app_lib::prReview` (authored by the first agent's App). Reuses the `copilot_dispatch.ts` dispatch primitives (`run`/`buildAgentEnv`/gemini-home+Vertex/API-key fallback/output parsers). Read-only (codex `-s read-only`, gemini plan mode); never mutates the target. Config: `iac_review` section in `stark_config_lib.ts` (`DEFAULT_IAC_REVIEW`).
- `tools/refactor_planner.ts` (CLI) + `refactor_planner_lib.ts` (orchestrator) — multi-agent **repository refactor-planning** dispatcher backing `/stark-refactor-plan`. Modes: `dry-run` (deterministic inventory + planned jobs, no LLM), `run` (10 focused subagents → `REFACTOR_PLAN.md` + `REFACTOR_BACKLOG.json`), `validate` (gate an existing backlog: schema + enum + unique/sequential ids + `depends_on` DAG + path-existence). Splits analysis into per-agent **context packs** (size-capped) instead of one whole-repo context. Supporting modules: `refactor_planner_discovery.ts` (deterministic host scan → `RepoInventory`: tree, languages, commands, import graph, cycles, TODOs, god-modules), `refactor_planner_context.ts` (per-agent packs + cycle detection), `refactor_planner_provider.ts` (`AgentProvider` interface + claude/codex/`noop` providers — provider/model from `--provider`/`--model`/`REFACTOR_PLANNER_*` env, never hardcoded; wraps `copilot_dispatch.ts` primitives), `refactor_planner_synth.ts` (host-owned conflict resolution + deterministic `PlanModel` assembly — tests-before-moves-before-deletes DAG), `refactor_planner_artifacts.ts` (renders the 14-section plan + builds/writes the backlog; planning-only writer), `refactor_planner_schemas.ts` (types, enums, runtime validators, the backlog gate). Prompts: `global/prompts/refactor-planner/*.md` (10 subagents). `noop` provider runs the whole pipeline with zero LLM calls (valid empty artifacts) for tests/CI. Usage doc: `skill/stark-refactor-plan/references/dispatcher.md`.

### Agent utilities
- `tools/claude_utils_lib.ts` — Claude CLI dispatch helpers (clean env via `runtime_env_lib`, headless command builder, model pinning)
- `tools/codex_utils_lib.ts` — Codex CLI dispatch helpers (JSONL parsing, reasoning-effort config)
- `tools/gemini_utils_lib.ts` — Gemini CLI dispatch helpers (isolated `GEMINI_CLI_HOME`, Vertex-AI env, API-key fallback, JSON output parsing)
- `tools/agent_disabled_error.ts` — shared `AgentDisabledError` raised when an agent is config-disabled
- `tools/agent_claude.ts` — the claude reviewer agent. **Supports CLI-enforced structured output:** set `BuildContext.jsonSchema` and `buildCommand` emits `--json-schema <inline JSON>`, which forces a conforming reply and retries the model on mismatch. `parseOutput` consumes it directly — a schema forces ONE conforming value, not the JSONL one-finding-per-line stream, so it reads `structured_output` (accepting `{findings:[…]}` or a bare array) before falling back to the line scanner; without that branch a schema-forced reply parses to zero findings AND zero parse errors, which the dispatcher reports as a failed domain. Read it yourself with `extractStructuredOutput(rawStdout)` — the envelope carries a top-level **`structured_output` already parsed as an object** (`.result` stays a *string* of the same JSON, so reading `structured_output` skips a parse rather than duplicating one). **Pass RAW stdout, never `normalizeOutput(stdout)`** — normalizeOutput unwraps to `.result` and throws `structured_output` away, so that composition returns `null` on every run (pinned by a test). `null` means the field is genuinely absent (no schema, older CLI, non-envelope stdout) = fall back to tolerant text extraction (e.g. `refactor_planner_provider.ts::extractJsonObject`); an `is_error` envelope **throws** with the CLI's own message instead, so an exhausted account cannot masquerade as "no schema was passed". **Three gotchas, all verified against 2.1.220:** the flag takes **inline JSON only** — a file path fails with `--json-schema is not valid JSON`; it takes a JSON **object** only — `null`, `false`, a string and an array each exit 1 with `--json-schema must be a JSON object`, so `serializeJsonSchema` rejects non-objects up front with an attributed error; and it is Claude-only, so `BuildContext.jsonSchema` is inert for codex/gemini and safe to set unconditionally. Schemas are capped at **120 KiB** — the binding limit is Linux's per-argv-string `MAX_ARG_STRLEN` (32 × PAGE_SIZE = 128 KiB), *not* the looser macOS whole-vector ARG_MAX — so an oversized schema fails loudly here instead of as an opaque `E2BIG` from spawn on a CI runner.
- `tools/agent_env_lib.ts` — **single owner of the agent subprocess env allowlist** (`AGENT_ENV_ALLOWLIST`) plus the PATH backfill (`resolvedPath`). `agent_claude.ts` / `agent_codex.ts` / `agent_gemini.ts` / `optimize_skill_description.ts` all import it; they previously kept four identical copies and drifted. (`stark_review_lib.ts::DEFAULT_TEST_ENV_ALLOWLIST` is deliberately separate — the trusted test-command runner needs broader toolchain env than a reviewer subprocess.) **`USER` is load-bearing, not cosmetic** — without it the claude CLI cannot resolve its Keychain identity and degrades to an unauthenticated path, failing with `Not logged in · Please run /login` or `Credit balance is too low` depending on account state, with nothing pointing at the env as the cause. Reproduced deterministically against 2.1.220 (`env -i HOME=… PATH=… LANG=… TMPDIR=…` fails; adding `USER=…` succeeds, 3/3 both ways). `copilot_dispatch.ts` had already allowlisted `USER` in its own `DEFAULT_RUNTIME_ALLOWLIST`; that fix was never back-ported, which is the drift this constant ends. GH/Anthropic/OpenAI credential vars stay excluded so a reviewer subprocess cannot exfiltrate them — pinned by its own test, separate from the allowlist assertion.

### Infrastructure
- `tools/stark_config_lib.ts` — full config reader (DEFAULT_* sections, per-section accessors, deep merge; CLI `--model <agent>` prints the resolved model id, exit 1 on unknown agent) — see TS tools section
- `tools/asset_root_lib.ts` — the plugin-resolution seam. `assetRoot()`/`assetConfigPath()`/`assetPromptsDir()`/`assetToolsDir()` resolve immutable assets from `${CLAUDE_PLUGIN_ROOT}` (installed plugin) else `~/.claude/code-review` (fallback for direct non-plugin invocations); `assetRootForHome(home)` is the test-injection variant; `stateRoot()` always returns the `$HOME` tree for mutable state. Imported by the config/prompt hub tools (`stark_config_lib`, `stark_review*`, `copilot_dispatch`, `self_healer`, `skill_router`, `context_compactor`).
- `tools/main_module_lib.ts` — **symlink-safe entrypoint guard** (`isMainModule(import.meta.url)`). The naive `import.meta.url === pathToFileURL(process.argv[1]).href` idiom silently no-ops when a tool is invoked through the `~/.claude/code-review` symlink (Node resolves `import.meta.url` to the real path but `argv[1]` stays the symlink) — `main()` never runs, the CLI exits 0 doing nothing. Since that symlink IS the direct/cron invocation path (see `asset_root_lib`), this resolves both sides via `realpathSync` before comparing. Use it for every new entrypoint's run-as-main guard. (~20 tools still carry an equivalent inline `function isMain()`; converging them onto this helper is a follow-up.)
- `tools/runtime_env_lib.ts` — isolated subprocess env builder (allowlist, GitHub App token injection, process-scoped temp dirs, `CLAUDE_PLUGIN_ROOT` propagation to sub-agents)
- `tools/gemini_auth_lib.ts` — **single source of truth for headless-gemini model auth.** Modes: `oauth` (default — `selectedType: "oauth-personal"`, rides the logged-in Google account's **Gemini Code Assist seat** via the oauth cred files copied into the isolated home; needs `GOOGLE_CLOUD_PROJECT` on a Code-Assist-enabled project, resolved through `resolveVertexProject`), `vertex` (per-token Vertex + ADC, the former hardwired behavior), `api-key` (`GEMINI_API_KEY`; also the runtime degrade path on auth errors). Resolution: `STARK_GEMINI_AUTH` env > `models.gemini.auth` config > `oauth`. Consumed by the three gemini env-builders (`gemini_utils_lib`, `copilot_dispatch`, `agent_gemini` — the latter also copies the oauth cred files it previously never needed). Vertex env vars (`GOOGLE_GENAI_USE_VERTEXAI`/ADC) are actively kept out of non-vertex envs — inside the CLI they override the settings.json auth type.
- `tools/claude_auth_lib.ts` — **single source of truth for headless-claude model auth. ONE mode: `subscription`.** No `ANTHROPIC_API_KEY` is ever injected; the CLI rides the logged-in account's OAuth creds via `HOME`, billing the seat. The **metered-`api` mode is gone (2026-07-24)** — it was a standing footgun: a revoked `ANTHROPIC_AGENTS` key surfaced only as a generic `cli_error` after ~370s with no 401 anywhere in the subprocess output, and a *missing* one hard-failed dispatch even though the CLI authenticates fine on OAuth. `STARK_CLAUDE_AUTH` and `models.claude.auth` are **accepted-and-ignored** so old configs don't break; any value other than `subscription` warns to stderr. All 4 dispatch env-builders route through `applyClaudeAuth` (`runtime_env_lib`, `copilot_dispatch`, `agent_claude`, `optimize_skill_description`), which actively strips a stale key so API billing can't silently re-enable. `ANTHROPIC_AGENTS` has **no consumer left** — it was dropped from `subagent_env_allowlist` (PR #847); don't put it back. Codex/Gemini auth unaffected.
- `tools/vertex_config_lib.ts` — resolves the Vertex AI project/location for headless Gemini dispatch **without hardcoding a GCP project id in source**. `resolveVertexProject()` precedence: `STARK_GEMINI_VERTEX_PROJECT` env > `models.gemini.vertex_project` config (ships empty) > `GOOGLE_CLOUD_PROJECT` env > cached `gcloud config get-value project` > null (caller degrades to the `GEMINI_API_KEY` path; the "must specify `GOOGLE_CLOUD_PROJECT`" error is in the fallback patterns). `resolveVertexLocation()` = `STARK_GEMINI_VERTEX_LOCATION` env > config > `"global"` (preview models are global-only; ambient `GOOGLE_CLOUD_LOCATION` is deliberately ignored). Imported by `gemini_utils_lib.ts`, `copilot_dispatch.ts`, `agent_gemini.ts` — the three former hardcode sites.
- GitHub App auth lives entirely in `tools/github_app{,_lib}.ts` (TS) — mints installation tokens, imported directly by `runtime_env_lib.ts`.

### TUI & session
- The session-start/end TUI is gone. `/stark-session` now collects state via `tools/stark_session.ts` and Claude renders the briefing/summary itself — see "TS tools" below.

### Other

### TS tools (`tools/`)
- `tools/stark_session_lib.ts` + `tools/stark_session.ts` — `/stark-session` data collector. Pure-function collectors (git, gh, board, alerts, health, healer, persona, skills, session_state) with injected `run`/`readFile` deps for testability; CLI subcommands `start` and `end` print structured JSON for Claude to render. Each collector returns its slot or `null` on failure and pushes `{source, message}` to a shared `errors[]` accumulator. Session-state, persona, alerts, skill-suggestions, healer-canary collectors hit pure-TS siblings (`tools/session_state.ts`, `tools/stark_persona.ts`, `tools/alert_delivery.ts`, `tools/skill_router.ts`, `tools/healer_canary.ts`); the last few remaining Python shell-outs are scheduled for their own follow-up slices. Replaces the deleted `scripts/session_tui*.py` ANSI/box-drawing renderer.
- `tools/github_app_lib.ts` + `tools/github_app.ts` — GitHub App auth (sole implementation; the parallel Python module `scripts/github_app.py` was deleted on 2026-05-19). Mints installation tokens via `node:crypto` RS256 JWT signing (no npm deps), reads private keys from macOS Keychain via `security find-generic-password` with STARK_* env-var fallback for CI and GH_TOKEN fallback last. On-disk token + installation caches under `~/.cache/github-app-tokens/` (same JSON shape the Python wrote, 0600 perms, 5-minute early expiry). Surface includes REST helpers (`apiGet/Post/Put/Patch/Delete`), `graphql()` with single-retry on transient connection errors, and per-owner installation-ID discovery. CLI subcommands: `token`, `repo`, `pr {list|view|create|review|merge|comment}`, `issue {list|create}`. Consumers: every SKILL.md bash snippet that needs a GH App token (swept on 2026-05-18) plus `tools/runtime_env_lib.ts`, which imports `getToken` directly.
- `tools/github_projects_lib.ts` + `tools/github_projects.ts` — GitHub Projects V2 GraphQL operations (parallel TS implementation of the deleted `scripts/github_projects.py`). All GraphQL goes through `tools/github_app_lib.ts::graphql()` so per-app auth + retry-once is inherited. Surface: `findProject`, `addIssueToProject`, `getFieldIds` (per-project field-id cache, lazy), `setField` / `setFields` (100ms throttle between mutations, matches the Python), `getItemFields`, `getItems` (paginated 100/page + client-side `itemMatchesFilters` for top-level + field-bag keys), `findItemForIssue`, `getIssueNodeId`, `isLegalTransition` + `LEGAL_TRANSITIONS` state graph, `transitionStatus` (idempotent no-op when already in target; throws on illegal transition with the allowed-set in the message), `checkSpecCompleteness` (Risk + AI Suitability gates, plus Spec Approval gate for high-risk items), `loadProjectConfig`. CLI ships 12 subcommands mirroring the Python's surface — JSON-first output for `jq` pipelines. The Python module + its tests were deleted as part of this slice (no Python orchestrator imported them; only test files did).
- `tools/preflight_lib.ts` + `tools/preflight.ts` — pre-flight environment validator (replaces the deleted `scripts/preflight.py`). All 14 checks ported with identical statuses (verified by check-by-check JSON diff against the Python on `stark-review` workflow). Same `overall ∈ {ready, degraded, blocked}` aggregation, same critical-vs-non-critical escalation rules, same `~/.claude/code-review/preflight.jsonl` append. CLI: `--workflow NAME --json --skip-check NAME...`. Pure logic (`aggregateOverall`, `resolveOpenaiApiKey`, `checkCostHardStop`, `checkDeprecatedConfig`, `checkStaleLocks`, `renderTable`) is exported for unit testing; network-touching paths (`checkGithubApp`) and binary-dependent ones (`checkCliClaude` / keychain checks) exercised live.
- `tools/stark_config_lib.ts` — full TS port of the former `scripts/config_loader.py` (Python→TS migration); now the sole config implementation. Reads `~/.claude/code-review/config.json` with deep-merge against built-in DEFAULT_* sections (MODELS, RUNTIME, SELF_HEAL, VALIDATION_GATE, SKILL_ACTIVATION, CONTEXT_COMPACTION, COST, MODEL_RATES) and exposes a section accessor per section plus `getModelId` / `isAgentEnabled`. `getRedTeamConfig()` walks repo/org `.code-review/config.json` overrides with the same two-layer defense as the Python: dotted-path locked fields (`personas`, `model`, `enabled`, `agent`, `min_severity_to_block`, `halt_on_unresolved`, `allow_human_review_halt`, `stages`, all `fix_plan.*`, `fold.enabled`, `fold.model`, `audit.retain_full_text`, `audit.excerpt_max_chars`) are rejected with a stderr warning; non-dict overrides at a locked parent path (e.g. `fix_plan: "off"`) are rejected wholesale to prevent subtree replacement; unknown top-level keys are pruned with a warning. `discoverConfig` is a minimal hierarchical merge that only consumes `cfg.agents` (preflight's only need).
- `tools/lock_helpers_lib.ts` — lock-file staleness helper (`isLockStale`). Same on-disk JSON contract (`{pid, start_time, timestamp, ttl_minutes, ...}`) and staleness rules (malformed timestamp / TTL exceeded / non-integer pid / `kill(pid, 0)` ESRCH / `ps -o lstart=` start-time mismatch → stale). The former `scripts/lock_helpers.py` was deleted on 2026-05-20 — no Python orchestrator imported it.
- `tools/skill_lib.ts` — shared skill discovery + reference parsing
- `tools/skill_audit.ts`, `skill_validate.ts`, `skill_optimize.ts`, `skill_autopilot.ts` — meta-tooling
- `tools/skill_diet.ts` — duplication linter for shared boilerplate (preflight, dispatch-failure, GH App auth)
- `tools/skill_smoke_test.test.ts` — runs on every `npm test`. Walks every `skill/stark-*/SKILL.md`, asserts frontmatter parses + `name:` matches dir name + every in-repo `tools/*.ts` and `scripts/*.py` reference resolves + every distinct TS CLI mentioned by any skill exits cleanly on `--help`. Cross-repo references (e.g. `~/Code/21Stark/stark-insights/...`) are filtered by an explicit `CROSS_REPO_PREFIXES` allowlist; add to that list if a skill ever points at another sibling repo.
- `tools/release_changelog.ts`, `release_version_bump.ts` — stark-release Steps 3 + 5
- `tools/review_setup_worktree.ts`, `review_cleanup_worktree.ts` — stark-review worktree provisioning
- `tools/housekeeping_infra.ts` — stark-housekeeping Phase 5 (sessions, locks, **statusline state**, log rotation, archival, **asset-symlink self-heal**). `findStaleStatuslineStateFiles()` sweeps the per-run/session statusline state files under `~/.claude` (`.statusline-procstart-<pid>`, `.statusline-lastreply-<sid>`, `.statusline-prompt-<sid>` — the last written by the `UserPromptSubmit` hook `config/statusline-prompt-hook.sh`, which stamps prompt-submission time so line 3's prompt clock + elapsed counter share one origin event) older than 14 days — one accretes per Claude Code process/session; the single-file caches (`.statusline-git-dirty-cache`, `.statusline-account-cache`) are excluded (they self-refresh, never multiply). Folded into `CleanupReceipt.statuslineStateRemoved`. `healAssetSymlinks()` repoints the legacy `~/.claude` stark-skills asset links (the 8 in `ASSET_SYMLINKS`: `code-review/{prompts,tools,config.json,scripts,standards,orchestrator.md}` + `plugins/stark-gh` + `output-styles/concrete.md`) when they go **dangling** or their target still carries a renamed path segment (`STALE_SEGMENT_RENAMES`, e.g. the 2026-07-11 `Code/Playground/`→`Code/21Stark/` reorg). Repoints only when the corrected target exists; otherwise reports it in `errors` and never deletes the link. Folded into `CleanupReceipt.symlinksRepaired`; honors `dryRun`.
- `tools/copilot_dispatch.ts` — stark-copilot lead/wing dispatcher (replaces former `scripts/copilot_dispatch.py`). Also the canonical home for shared agent-dispatch primitives now imported by `iac_review_lib.ts` (`run`, `buildAgentEnv`, `setupGeminiHome`, `makeGeminiEnv`, `tryGeminiApiKeyFallback`, `releaseAgentTempDir`, plus the verdict parsers). **2026-07-25 surgery (autopsy F1/F2):** `DEFAULT_MAX_ROUNDS` 4→1 (retry budget past first-failure buys churn, not fixes); goal mode applies to **round 1 only** — fix rounds are single-pass targeted patches even with `--goal-condition`; the test command **auto-detects** from the trusted repo root when unset (`stark_review_lib.ts::detectTestCommand`, runs in the worktree after every round) and a wing `approve` over red/never-ran tests returns `unresolved` + `error=approved_but_tests_red` — the runnable check outranks the verdict; `--diff-out PATH` writes the final diff to disk, sets `final_diff_path`, and blanks the inline `final_diff` so sequential mode stops round-tripping hundreds of KB through model context (parallel's stdout-redirect already did).
- `tools/stark_persona_lib.ts` + `tools/stark_persona.ts` — pure-TypeScript implementation of `/stark-persona` (replaces the deleted `scripts/stark_persona.py`). Library covers the full surface (PersonaRecord + roster parse, active.json read/write/delete, `computeWeight`, `getDateMatches`, `fuzzyMatchPersona`, `initDb` + schema, `syncWeights`, `selectSinglePersona`, `selectCombo`, `recordRating` with combo dilution, `recordSurveyAnswer`, `addPersona` with sanitization, `makeRandom` Mulberry32, `SURVEY_POOL`). CLI ships 11 subcommands: `select` / `deactivate` / `rate` / `survey` / `survey-answer` / `add` / `stats` / `history` / `print-roster` / `print-weights` / `session-end`. `/stark-session` start/end call it via `node --experimental-strip-types tools/stark_persona.ts …`.
- `tools/stark_handover_lib.ts` + `tools/stark_handover.ts` — `/stark-handover` storage engine. Persists handovers under `{root}/{project}/{worktree}/{task}/` — a numbered `handover_{N}.md` chain (exclusive sequence allocation, private file modes, frontmatter with `prev` link) plus a rewritten-wholesale `PROGRESS.md` tracker — so a session can `/clear` and resume from disk. Lib: `sanitizeSlug`, `resolveRoot` (env > config, `~` expansion), `deriveGitContext` (project = basename of the dir holding the resolved `--git-common-dir`, identical from main checkout and linked worktrees; worktree = linked-worktree dir basename else branch slug), `chainFiles`/`nextSeq` (max+1), `listTasks`/`pickTask` (mtime-desc; default = most recent), `saveHandover`, `resumePayload`. CLI: `resolve|save|resume|list [--all]`, JSON stdout; save requires both `--handover-file` and `--progress-file`; resume returns `task_slugs` alongside the detailed task objects used by resolve/list. Config: `handover` section (`DEFAULT_HANDOVER`, root `~/Code/Handovers` — deliberately user-space, NOT `stateRoot()`).
- `tools/session_id_lib.ts` + `tools/session_id.ts` — pure-TypeScript session ID resolver (replaces the deleted `scripts/session_id.py`). Three-tier resolver: `CLAUDE_SESSION_ID` env > `~/.claude/projects/` newest-mtime marker scan > uuid4. Consumed by `tools/session_state_lib.ts` and `tools/context_compactor_lib.ts`.
- `tools/session_state_lib.ts` + `tools/session_state.ts` — pure-TypeScript session state machine (replaces the deleted `scripts/session_state.py`). Same on-disk JSON shape under `~/.claude/code-review/sessions/{sanitized-id}.json`, same path-traversal sanitization, same git-derived defaults + GitHub URL normalizer. CLI: `[--session-id ID] [--json]` (Python parity) plus `set --field <name|start_head|last_checkpoint> --value VAL` for the SKILL.md Phase 3 / Phase 6 mutators.
- `tools/self_healer_lib.ts` + `tools/self_healer.ts` — pattern-based auto-fixer (replaces the deleted `scripts/self_healer.py`). Given a stderr capture and a pattern id, walks the gate ladder (guard → max_per_session → auto-mode allowlist → circuit breaker → suggest/auto branch → execute → record outcome → emit alert on critical transitions). Same gates + same on-disk file shapes as the Python; CLI surface unchanged. Atomic writes for session + circuit state. Emits alerts through `tools/alert_delivery_lib.ts`. Consumed by `skill/stark-phase-execute/SKILL.md` via subprocess.
- `tools/healer_canary_lib.ts` + `tools/healer_canary.ts` — canary rollout for `self_healer` patterns (replaces the deleted `scripts/healer_canary.py`). Decides when a `suggest`-mode pattern has earned its way to `auto`-mode by reading the on-disk performance log + circuit-breaker state. CLI: `--status` (Python parity) + three new subcommands beyond the port: `--check` (exits 2 for oncall paging if any auto-mode pattern's circuit is open), `--close-circuit PATTERN_ID` (manual recovery), `--explain PATTERN_ID` (audit trail). Atomic config writes (the Python's `_write_config` was naive RMW). Configurable promotion gate via `config.self_heal.{min_successful_suggests, abort_window_days, circuit_open_hours}`.
- `tools/skill_router_lib.ts` + `tools/skill_router.ts` — pure-TS contextual skill suggestions (replaces the deleted `scripts/skill_router.py`). Maps `context ∈ {review, implementation, session, debug}` to skill candidates, skips suppressed + recently-used ones, ranks by `relevance + age` score, caps at `max_suggestions`. Inline `skill_activation` config loader. Consumed by `/stark-session` collector + `stark-phase-execute` end hook.
- `tools/alert_delivery_lib.ts` + `tools/alert_delivery.ts` — pure-TS alert emission + check (replaces the deleted `scripts/alert_delivery.py`). On-disk contract unchanged: `~/.claude/code-review/alerts.jsonl` + `alert-{ts}.marker` files in the same dir, same-second collision counter. Consumed in-process by `tools/self_healer_lib.ts`; CLI (`--check [--json]`) consumed by the `/stark-session` collector.
- `tools/context_compactor_lib.ts` + `tools/context_compactor.ts` — pure-TypeScript session-checkpoint generator (replaces the deleted `scripts/context_compactor.py`). Writes `checkpoint-{ts}.md` under `sessions/{sid}/`, updates `session_state.last_checkpoint`, honors `max_checkpoint_size_kb` truncation cap. Loads the `context_compaction` config section inline (no `config_loader.py` dependency). CLI parity preserved (`[--session-id ID] [--json]`). Consumed by `/stark-session` Phase 3b plus stark-copilot and stark-phase-execute end hooks.
- `tools/optimize_skill_description.ts` — skill-description optimizer (replaces the deleted `scripts/optimize_skill_description.py`). Pure helpers (`parseSkillDescription`, `buildImprovePrompt`, `buildCleanEnv`) are unit-tested; scoring still shells out to the skill-creator plugin's Python `run_eval.py` (not owned by this repo), then `proposeImprovement` calls `claude -p` with the failing-query feedback. CLI preserves the Python's flag surface and JSON report shape — operators run it manually, no automation depends on it.

### Config & prompts
- `global/config.json` — default config schema (models, runtime, triage, cost, etc.)
- `global/prompts/{claude,codex,gemini}/` — per-agent PR review prompts, **rewritten 2026-07-26 against the codereview dossier** (vault `30_Research/`; telemetry: 30 PRs/349 findings — noise was concentrated in ssot, severity was central-tendency mush). **5 domains** (was 7): `architecture, behavior, security, test-coverage, spec-conformance` — behavior absorbed type-safety (runtime-affecting unsoundness only), spec-conformance absorbed a narrowed ssot (second-source-of-truth requires naming BOTH the copy and the owner). Each domain file is a failure-mode rubric (5–9 named modes + this lens's noise-attractor bans + evidence line) and is **byte-identical across the three agent dirs** — edit all three together; `agent.md` = ≤15-line per-agent dialect header + the shared review core (mission/A3, scope, playground guard, **context duty** (read the full post-change file — 48% of FPs are missing-context), **evidence contract** (quoted span + concrete failure scenario or don't emit), **deterministic severity ladder** (consequence-anchored, no gut ratings, no exemplars — models compress ordinal scores), emission order (evidence before severity), dedup, large-diff ladder). No model ids in prompts (drift rot). `classifier.md` (per-finding fix/noise disposition — the telemetry source) unchanged; codex `fixer.md` unchanged.
- `global/prompts/copilot/` — `/stark-copilot` lead/wing `implement`/`review` prompts
- `global/prompts/iac-review/` — canonical multi-agent IaC review rubrics: `terraform.md` (failure-mode checklist + version→feature guard) and `terragrunt.md` (orchestration-layer checklist + Terraform-scope handoff). Loaded by `tools/iac_review_lib.ts` via `assetPromptsDir()`; the `/stark-terraform-review` + `/stark-terragrunt-review` skills are thin wrappers over the dispatcher.
- `global/prompts/triage/` — domain triage prompts and manifest
- `standards/templates/` — PR template, ADR template, MkDocs scaffold, staleness config
- `standards/index.md` — "Start Here" pitch page for adopting the doc system

## Commands

```
# In Claude Code — install/update from the marketplace:
/plugin marketplace add 21StarkCom/bifrost
/plugin install stark-analyze@bifrost   # + stark-plan, stark-implement, stark-gh, stark-ops, ...
/plugin update  stark-analyze@bifrost   # pull the latest published version
```

## Skills

All skills live in `skill/stark-*/SKILL.md` and are packaged into marketplace plugins (see Distribution above).

### Pipeline (end-to-end, in order)

- `/stark-author <intent|notes-path> [--tier skip|short|full] [--out PATH] [--no-advisory] [--ready]` — **Stage 1 (evidence-based rewrite): human-gated spec+plan authoring in ONE session.** Tier check (skip/short/full by uncertainty, not length) → time-boxed recon (~10–15 tool calls) → structured interview (fixed question order, EARS-shaped behavior contract, voiced ambiguities capped at 3, five-part stopping rule) → one self-contained doc (`docs/specs/YYYY-MM-DD-<slug>-spec.md`: intent, IN/OUT boundary, non-derivable repo context, EARS criteria, task DAG sized in tens of human-minutes with machine-checkable done-whens, closing verification command, append-only Deviations; plus a NON-normative plain-English operator digest sidecar `docs/specs/YYYY-MM-DD-<slug>-spec.human.md` — **gate-shaped, not a prose summary**: one fixed section per gate checklist item, carrying the artifacts the tripwire-bearing items bite on (interfaces, verification command, unwanted-behaviour criteria, per-task done-whens) **verbatim in code spans**, under a *material-never-verdicts* rule that bans pre-supplying any item's answer (false-PASS shape, missing-OUT, weak done-when, cuttable task, risk reassurance); ≤60 lines Full / ≤20 Short, regenerated on every revision — spec wins on conflict) → at most ONE zero-shared-context advisory subagent pass (findings go to the human once, never a loop) → human gate (8 value-stating checklist items, <60 min/<400 lines, rubber-stamp tripwires) → pin (`accepted-base` commit hash) → draft PR (`spec/<slug>`, stark-claude) → fresh-session handoff contract. **No LLM-reviews-LLM loop anywhere; no growth breakers needed because there is no loop to contain.** For new work it replaces write-spec → review-spec → red-team-spec → spec-to-plan → review-plan. Evidence: `skill/stark-author/references/stage1-dossier.md` (61 claims, 3-vote adversarial verification). Skill `skill/stark-author/SKILL.md` — pure protocol, zero new TS machinery. **Phase 3 carries a wiring-seam checklist (added 2026-07-30)** — the highest-value fix from the `hibob-profiles-cache` run, where **5 of 8 tasks** had to touch a file outside their declared set, every one the same category: a seam without which the task's work is inert. Before a file set is final, scan four classes: **registration/dispatch** (migration step lists, capability registries, route tables — a migration `.sql` is inert data until the step list runs it), **generated artifacts pinned by tests** (schemas/catalogs/generated docs that adding a struct field makes stale by construction), **call sites** (a new helper with zero callers is dead code behind a green gate — the T6 failure: an audit helper would have shipped with the behavior clause holding nowhere, and the gate would have passed), and **doc/description generators** (where the emitted text actually lives). The closing authoring test: *"if this task's diff were applied and nothing else, would the behavior criterion actually hold at runtime?"* A done-when can pass while the answer is no — that is the vacuous-green the whole two-stage design exists to prevent.
- `/stark-build <spec-path> [--dry-run] [--no-advisory] [--no-fix] [--stay-draft] [--max-turns N] [--model ID]` — **Stage 2 (evidence-based rebuild): autonomous implementation from an accepted `/stark-author` spec** (the two-stage architecture replacing the loop pipeline below). The interactive session is the RUNNER: it dispatches one fresh headless `claude -p` session per spec task, **gated by checks the agent cannot edit** — a PreToolUse path-deny hook write-protects the spec + gated existing tests + harness scripts (`skill/stark-build/references/hooks/protect-paths.sh`), and a Stop-hook gate blocks turn-end while the task's done-when check is red, flipping to **abort-with-deviation at 7 consecutive blocks** so Claude Code's 8-block override never lands a silent green (`references/hooks/stop-gate.sh`). Abort is a first-class success (measured 54%→9% cheating cut); anti-cheat prompting is never the gate. Sequential single writer; worktree at the spec's `accepted-base`; branch `build/<slug>` + immediate draft PR via `tools/copilot_land.ts` (create-or-adopt, never force); one commit per green task; crash/timeout = ONE resume from PROGRESS.md + commits, never from zero. The runner verifies every task deterministically (re-runs the check itself, diff-vs-protected-list tamper check, diff-vs-declared-file-set scope check). The spec's closing verification command is a **held-out e2e gate** (task-green/e2e-red divergence = the gaming/mis-decomposition signal; PR stays draft). ONE cross-vendor advisory review (codex, read-only, full diff + spec) posts severity-tagged findings to the PR (pinned `SEVERITY: critical|high|medium|low` ladder so the filter is machine-readable), followed by **Phase 4b — exactly ONE fix round over the medium+ findings** (`--no-fix` opts out): a single fresh headless session under the same hook harness, e2e as its Stop-gate check, told to fix only what the findings name and to *reject* a wrong finding with a logged reason rather than fake it. The runner then re-runs every task check + e2e itself and reverts the fix commit on any regression — **the advisory reviewer is never re-run, there is no round 2, and `low` + anything still open dies at the human**. A rejection is a claim, not a resolution: any medium+ left open keeps the PR draft. Fix-round yield (selected/fixed/rejected/regressed) is the kill-switch metric. State + evidence under `~/.claude/code-review/history/build/<slug>/<run-id>/`, never the repo. Protocol skill + two POSIX hook scripts. Evidence: `skill/stark-build/references/stage2-dossier.md` (9 RQs, measured anti-cheat data). Skill `skill/stark-build/SKILL.md`. **Six harness defects fixed 2026-07-30** from the `hibob-profiles-cache` run: (1) **every dispatched subprocess closes stdin** (`</dev/null`) — both `claude -p` and `codex exec` read stdin *in addition to* the prompt arg, and an orchestrated shell's stdin is a pipe that never EOFs, so `codex exec` hung 5h14m emitting 39 bytes; the rule covers `stop-gate.sh`'s own check invocation too (a Stop hook's stdin is the payload pipe), while `protect-paths.sh` is the deliberate exception since it must read the payload; (2) the Phase-2 prompt directed evidence into `$STATE/tasks/<id>/`, which `protected.list` blocks — evidence now goes to `$STATE/evidence/<id>.log`; (3) **long gates run backgrounded and stop via task-stop, never a foreground timeout**, which leaks the child tree so each retry competes with its predecessors (four concurrent check.sh, oldest 52min) and reads as external contention; (4) `bash check.sh` proved unreliable where its own content was not, so the runner may run the done-when **verbatim and unmodified** when the wrapper won't return (root cause undiagnosed — determinism is preserved because the command is identical and still runner-run) — plus **macOS has no `timeout(1)`**, so a `timeout`-wrapped sweep reports every gate as a false red in 0s via exit 127; (5) broad kill patterns (`pkill -f "go test"`) match command-line text, not ownership — they killed the runner's *own* in-flight gate (manufacturing a misread `rc=143`) and a *different* concurrent run's task; (6) `prepare-branch` now takes `--require-base` (see its entry above) and Phase 1 asserts the pin independently. **Nine further defects fixed the same day from an xhigh review of that fix** — the two that could still land a false green: `protected.list` directory entries written **with a trailing slash** built the glob `<dir>//*`, which matches nothing, so the Edit/Write deny branch passed every file under it and a task session could rewrite its own `check.sh` (`protect-paths.sh` now normalizes trailing slashes itself — the runner is an LLM filling a template, so the hook cannot depend on the prose); and the wrapper-flake fallback said to re-run the done-when "verbatim from the spec", which drops the wrapper's `set -euo pipefail; cd <wt>` envelope and so greens a task against the **main checkout** (it now runs the check file's whole content). Also: the runner's own gate invocations keep the `bash` prefix (`check.sh` has no shebang and is never `chmod +x` — direct execution is a 126 false red on every task) and close stdin (the runner's stdin never EOFs, which is the likeliest explanation for the undiagnosed wrapper flake); the `perl -e 'alarm N; exec'` timeout substitute was **removed** — `alarm` signals only the exec'd shell, orphaning exactly the `go test` children the paragraph above it bans, so a hard bound must kill the process **group**; and `pgrep -af` was replaced with `pgrep -lf`, since on macOS `-a` means *include ancestors* (it prints bare PIDs and widens the set to the orchestrating session, so killing that list ends the run). For implementation from an accepted spec it replaces copilot's review loop, plan-to-tasks, and phase-execute.
- `/stark-copilot <plan-or-prompt> [--lead AGENT] [--wing AGENT] [--plan-slug SLUG] [--no-goal] [--sequential] [--parallel] [--ready]` — autonomous implementation with paired lead/wing subagents (default lead `claude`, wing `codex`); lead implements in worktree, wing reviews diff, **one fix round by default** (`--max-rounds` 1; unresolved goes to the human, not another loop). When lead is `claude`, the implement prompt is `/goal`-prefixed by default (`--no-goal` to disable) — **round 1 only; fix rounds never goal-loop**. Tests auto-detect + host-run per round; **approve over red tests does not land** (`approved_but_tests_red`). §2b passes `--diff-out` so the final diff rides disk (`final_diff_path`), never model context. **Plans its own execution first (§1.2.7 + §1.4):** builds a **task-level** dependency DAG (issue `## Dependencies` / phase `depends_on` / plan-text edges; ambiguous ⇒ dependent, fail-closed; open human-led dependency ⇒ dependents skipped, not run), **chain-collapses** linear task runs into single steps (a fully-linear phase = one step = zero overhead regression) and levels the steps into waves with phase barriers, then fans each multi-step wave out concurrently via a Workflow (one dispatcher loop per step, own worktree, per-step result files — diffs never round-trip through model output) — parallelism is on by default; `--sequential` disables it, `--parallel` force-collapses all steps into one wave. Wave apply is atomic (reset on failed 3-way, clean-tree precondition, seeded re-dispatch on cross-step conflicts); dispatcher `final_diff` is the `--binary --full-index` rendering while the wing reviews the textual diff. Issue-driven mode when task issues exist (formerly created by the retired plan-to-tasks stage). **Lands an impl PR (§1.7/§2.6, via `tools/copilot_land.ts`)** — historically copilot only committed steps locally and never opened a PR. Before the first step commits, §1.7 adopts-or-creates a deterministic `copilot/<slug>` branch (`copilot_land.ts branch-name` + `prepare-branch`, ff-only, never force) so every step's commit lands there; after all waves + end-of-run verification, §2.6 pushes (never `--force`) and adopts-or-opens the PR (`copilot_land.ts land`, draft by default, `--ready`/`--no-draft` opts out, authored by the lead's GitHub App). §4b posts the run summary to that same resolved PR — it no longer runs its own competing `gh pr view` detection. Emits the pipeline's `STARK_STAGE_SUMMARY` completion line (`standards/stage-completion-line.md`) as the last line of output, carrying `plan_slug` + the complete `prs` array (newly opened plus adopted). **Playground-scope guard:** the copilot `implement`/`review` prompts (`global/prompts/copilot/{claude,codex,gemini}/`) carry the scope discipline — the lead doesn't build production ceremony (auth/HA/retries/audit/migration) the step never asked for, and the wing treats over-engineering as an `over-engineering` finding to cut instead of demanding more machinery.
- `/stark-gh:pr-merge [--pr N] [...]` — rebase + draft squash prose & CHANGELOG entry via Codex + force-push + **mark a draft PR ready-for-review** (fires the target CI) + squash-merge once CI is green. A repo with **no root CHANGELOG.md skips the changelog step** (edit/commit/restore) instead of dying with exit 15; a CHANGELOG that exists but lacks `## [Unreleased]` is still the hard error — malformed, not absent. **The squash subject inherits the PR title's ticket prefix** (STARK-229): a lower-case `type(TICKET-<n>):` title (`feat(STARK-193):`) makes that prefix mandatory on the drafted subject — enforced as a token in `lib/draft_schema.ts` (one space + non-empty summary, no repeated prefix, an added `!` allowed, and the ≤72-char cap measured *after* the prefix so the two constraints can't collide inside the 2-attempt budget), retried once, then **abort with `DRAFT_INVALID`** rather than landing a prefix-less commit on main. The ticket key must look like one (`STARK-229`, `EI-1234`) — an ordinary scope like `docs(adr-0007):`/`feat(gpt-5):`, or a capitalized type, mints **no** requirement, so repos without a ticket convention are never merge-blocked
- **`/stark-gh:pr-open` gates the PR TITLE on a ticket, per-repo opt-in** (STARK-247) — the front half of the rule above, since a title with no ticket gave the merge side nothing to inherit. A repo declares the convention in **`.stark-gh.json`** at its root (`{"requireTicketScope": true, "ticketKey": "STARK"}` — **`ticketKey` is mandatory when enforcing**, since a keyless scan fabricates tickets from version tokens like `AWS-2`); preflight resolves the ticket from the **branch name** (case-insensitive against `ticketKey`, underscore-separated, so `stark-247`/`worktree-STARK-229`/`feature_STARK-247` all resolve) and pins it into the drafter's prompt + output validation. An explicit `--title` is **validated, never rewritten — on create AND on an existing-PR title edit** (pr-open writes `--title` onto an existing PR, so exempting that path bypassed the gate): missing / mismatched / wrong-case ticket exits **33**, as does a new PR with no resolvable branch ticket; an existing PR whose title is untouched is not gated. **Default off** (stark-gh runs on Evinced/OSS repos where `STARK-` is meaningless), but a **present-but-broken** `.stark-gh.json` — bad JSON, wrong type, or enabling without `ticketKey` — is a **fatal exit-33 error, not a silent revert to off** (a gate you opted into fails closed). There is still **no CI title check** — `lib/ticket.ts` is the only enforcement that runs
- `/stark-gh:cleanup [--pr N] [--dry-run] [--keep-branch NAME] [--no-rebase] [--no-watcher-cleanup] [--no-config] [--no-gc] [--drop-stale-stashes] [--force] [--json]` — sweep merged/stale branches (local + remote), prune tracking refs, remove worktree leftovers (including detached-HEAD `review-*-prN-*` worktrees for done PRs), clean merged-PR watcher state, surface stale stashes, `git gc` loose objects, and rebase current branch onto upstream so history stays linear. `--pr N` narrows to one PR's head ref + watcher state.
- `/stark-review [PR_NUMBER]` — single-agent PR code review (1 LLM × triage-selected domains from 5, fast/cheap; triage skips low-confidence domains entirely — skipped is logged, never posted). The fix-loop test gate's `test_command` is **auto-detected** when unset: `stark_review_lib.ts::detectTestCommand()` reads the **trusted** `--config-root` checkout only (never the PR worktree — RCE guard) and recognizes Makefile `test:`/`npm test`/`go test`/`cargo test`/node:test `*.test.ts|js`/pytest-with-tests; resolution order is explicit `config.test_command` → detected → none (soft-skip via `allow_no_test_command`). Repos no longer pin a brittle `test_command` (the old `pytest scripts/` left from the Python era exited 5 every run). **Playground-scope guard:** the per-agent review preamble (`global/prompts/{claude,codex,gemini}/agent.md`, prepended to all 5 domains) carries the scope-proportionality block — a declared single-user/playground project is not held to auth/RBAC/HA/audit-log/migration/adversarial-input/exhaustive-test standards; real defects (crash/data-loss/wrong-output/broken-contract/scope-relevant-security) always stay in scope. Stops junk "add production hardening" comments on playground PRs.
- `/stark-review-improvement [--prompts-dir DIR]` — improve prompts based on review assessment (PR or spec/plan review)
- `/stark-terraform-review [path] [--agents a,b] [--changed] [--no-tools] [--min-severity ...] [--pr N --repo O/R] [--dry-run] [--json]` — **multi-agent** Terraform/OpenTofu (HCL) review against a path: runs the failure-mode rubric across one or more configurable LLMs (each its own subagent), merges + cross-validates findings. Host runs read-only scanners (`terraform fmt/validate`, `tflint`, `trivy config`, `checkov`) as evidence. Thin skill → dispatcher `tools/iac_review.ts --kind terraform`; rubric in `global/prompts/iac-review/terraform.md` (version-aware guard included). Agents resolve `--agents` > config `iac_review.agents` > `["codex"]`. Rules adapted from HashiCorp Style Guide (MPL-2.0) + Anton Babenko terraform-skill (Apache-2.0) + TerraShark; research in `docs/specs/2026-06-24-terraform-terragrunt-review-research.md`.
- `/stark-terragrunt-review [path] [--agents a,b] [--changed] [--no-tools] [--min-severity ...] [--pr N --repo O/R] [--dry-run] [--json]` — **multi-agent** Terragrunt **orchestration-layer** review (include/dependency/generate/remote_state, mock-output schema, DAG cycles, values/DRY, state isolation, stack composition, git source refspec). Host scanners: `terragrunt hcl validate`, `terragrunt find --dag --dependencies`. Defers resource/module HCL to `/stark-terraform-review`. Thin skill → `tools/iac_review.ts --kind terragrunt`; rubric in `global/prompts/iac-review/terragrunt.md`. Same configurable `iac_review.agents`. Adapted from jfr992/terragrunt-skill (Apache-2.0) + TerraShark.

### Workflow & Ops

- `/stark-fresh-eyes <doc-path> [--focus "aspect"] [--model opus|fable]` — **one-shot zero-context review of a prompt/brief/spec/doc before it ships.** Dispatches ONE read-only subagent (Fable by default) with zero shared context that re-verifies every checkable claim by a **DIFFERENT method** than the doc's own (recount recursively, recompute numbers from raw sources, test paths, `--help` cited commands) and reports defects only — wrong facts with measurements, contradictions, two-reading ambiguities, unresolvable references, cannot-verify claims, missing definitions. **One dispatch per document revision, findings dispositioned once (fix/reject/accept-as-known), never a round 2** — fresh eyes work through method difference, not repetition (the 39-vs-34 file-count lesson; LLM-review loops are non-convergent per the 2026-07-25 autopsy). Zero findings is a valid output. Pure protocol skill, zero TS. Skill `skill/stark-fresh-eyes/SKILL.md`.
- `/stark-ssot [file-or-area]` — **SSOT consolidation workflow**: give a duplicated value/rule/calculation ONE owner and route the copies through it (discover the repo's existing owners first — config/registry/resolver/domain module; unify only when both places answer the same question; record deliberate duplication + its drift guard). **Detection vs workflow:** automated SSOT detection in PR review lives inside the `spec-conformance` domain (triage-selected) since the 2026-07-26 prompt rewrite — the standalone auto-fired `ssot` lens was retired after producing 18 of the fleet's 28 review-noise labels (45% noise; its 22 real fixes all had a nameable owner). The skill's review bar mirrors the domain's: an SSOT finding names BOTH sides (copy + existing owner, quoted) plus the concrete drift scenario; **no nameable owner → not a finding**. Skill `skill/stark-ssot/SKILL.md`.

- `/stark-session [start|end]` — session management: briefing on start, cleanup on end
- `/stark-handover [save|resume|status] [--task slug]` — cross-`/clear` session continuity: **save** mines the conversation into the next `handover_{N}.md` (numbered chain, frontmatter-linked `prev`) and rewrites the `PROGRESS.md` done-vs-todo tracker under `~/Code/Handovers/{project}/{worktree}/{task}/`; **resume** loads the latest handover + tracker in one call so a fresh session continues with zero recap; **status** lists tasks. Storage engine = `tools/stark_handover.ts` (root: `STARK_HANDOVER_ROOT` env > `handover.root` config > `~/Code/Handovers`)
- `/stark-handoff [write|list|use|launch] [name] [--type continuation|fork|fix|brainstorm|research] [--fresh-eyes]` — **the sibling of `/stark-handover`, and the distinction is the whole point: handover = disk state for resuming the SAME task in the same repo; handoff = a self-contained prompt file a DIFFERENT executor starts from** (a fresh session after `/compact`, a fix dispatched into another repo, a parallel fork, a brainstorm or deep-research brief). A resume-in-place ask routes to `/stark-handover` and stops. **write** (default) mines the conversation, picks one of five types → three skeletons (`continuation`/`fork` → execution, `fix` → investigation, `brainstorm`/`research` → inquiry), drafts against the shared spine (envelope/payload split, read-first pointers with line numbers, established-vs-NOT-established, binding constraints, anti-goals, evidence bar), self-checks against the 9-check research-prompt rubric, and writes `{root}/<slug>-prompt.md` with a paste-safe first-line header comment (`<!-- stark-handoff repo=… type=… date=… -->`) that `list`/`use`/`launch` grep. **list** tables every `*.md` directly under the root — the root is dedicated, so that non-recursive listing IS the selection rule, and hand-copied legacy files join by being copied in. **use** reads the file fully, renders a 5–8 line brief, recreates its task list and starts item one; bare `use` picks the newest header whose repo is this repo and **never crosses repos**. **launch** dispatches a backgrounded headless `claude -p` in the target repo with **stdin closed** (`</dev/null` — the stark-build 5h14m hang) to a timestamped log, `--permission-mode acceptEdits`, never `--dangerously-skip-permissions`; it refuses headerless files, ambiguous repo matches, and inquiry types (brainstorms need Aryeh interactive, research prompts run in web deep-research tools). `--fresh-eyes` is exactly ONE `/stark-fresh-eyes` pass, dispositioned once — never a round 2. Storage root: `STARK_HANDOFF_ROOT` env > `handoff.root` config > `~/Code/Handoffs` — user-space beside `~/Code/Handovers`, deliberately NOT `~/Code/.scratch` (freely purgeable, and a handoff file is the sole mission carrier until used). **Zero TS** — pure protocol skill + `references/` templates, guarded by `tools/skill_smoke_test.test.ts`; `disable-model-invocation: true`, like the sibling. Claude-session only: no Codex variant (`launch` dispatches `claude -p`).
- `/stark-release [patch|minor|major]` — cut a release: changelog, tag, GitHub Release
- `/stark-housekeeping [--dry-run] [--aggressive]` — audit and clean up stale issues, dead branches, worktree remnants; also self-heals dangling/stale `~/.claude` stark-skills asset symlinks (see `housekeeping_infra.ts`)
- `/stark-persona` — session character voices with weighted selection, combos, catchphrases, and feedback
- `/stark-gh-user [show|primary|secondary|swap|limits]` — **human-only** GitHub identity swap, for when `aryeh-stark`'s GraphQL/REST hourly bucket runs dry. `primary` = `aryeh-stark` (THE identity); `secondary` = a deliberately-provisioned relief account. Carries `disable-model-invocation: true` and **nothing calls `tools/user_token.ts` automatically** — a swap happens because Aryeh typed the command, never because an agent chose to. It works by exporting `GH_TOKEN`, which overrides `gh`'s keyring for every later call in that shell, so **revert (`unset GH_TOKEN GITHUB_TOKEN STARK_GH_USER`) as soon as the rate-limited command is done** or later PR activity silently authors as the relief account. Tokens in Keychain service `stark-gh-token`; resolver = `tools/user_token.ts`. **Currently unprovisioned** — all four entries absent as of 2026-08-04, so it fails until seeded.
- `/stark-cc-user [show|list|add <name>|use <name>|remove <name>|prune [--dry-run]|reset [--yes] [--snapshots]|limits|next [--dry-run] [--best]|order [names...]]` — switch the active **Claude Code account** when a 5h/7d window runs out. Profiles in macOS Keychain (service `stark-cc-token`); logic = `tools/cc_account_lib.ts`, CLI = `tools/cc_account.ts`. **A switch has two halves** — the Keychain `Claude Code-credentials` blob *and* `~/.claude.json`'s `oauthAccount`; writing one without the other leaves the CLI authenticated as one account while every `oauthAccount` reader (the statusline included) reports another. **The Keychain item is keyed by the login user** (`USER` env, `unknown` fallback — `cc_account_lib.ts::resolveClaudeKeychainAccount` mirrors the CLI's own resolution): the first release hardcoded account `root`, a stale relic entry, so every `use` wrote credentials the CLI never read and every `add` captured the same dead token — the whole tool was a silent no-op until 2026-07-29. Takes effect on the next `claude` launch. **The identity key is the SEAT — `accountUuid:organizationUuid`.** Neither component is unique: one address holds seats in several orgs (`aryeh.kiovetsky@evinced.net` is both an Evinced RD team member and the owner of a personal Max org) and one org holds many members (`aryeh.kiovetsky` and `aryeh.stark.1` both sit in Evinced RD). **Team-plan limits are per-member**, so every (account, org) pair has its own budget. Both narrower keys were tried in live use on 2026-07-29 and both lost data — email merged T0/M0 (#815), org alone merged T0/T1 (#817) — each time `add` evicted a sibling from the registry, the two shared a snapshot file and reported each other's usage, and `use` wrote one seat's credentials into the other's stored record. Registry entries from either earlier scheme backfill their seat key from the stored Keychain record on read. **Headroom for inactive accounts has no live source** — the 5h/7d percentages exist only in the statusline stdin payload, so `config/statusline-command.sh` snapshots them per-render to `~/.claude/.cc-usage-<accountUuid>_<orgUuid>` (fork-free redirect, `seat_key=` written last so a torn read — and any file left by an earlier keying scheme — degrades to `unknown` rather than to a falsely-low percentage). Ranking uses two facts: a window past its `resets_at` is *provably* empty (`reset`), and within a live window usage only rises, so a stale reading is a strict lower bound (`floor`). No snapshot ⇒ `unknown`, sorted **last** — never assumed free. **`next` walks a fixed rotation cycle** (set with `order`, stored per-profile in `~/.claude/.cc-profiles.json`), wrapping at the end and skipping profiles with no stored credentials; `add` appends rather than inserting. **`next` switches by default** — `--dry-run` previews the pick instead (`--apply` is still accepted and now just names the default; gating the switch behind it made the common case two commands and the bare form a no-op that read as a broken tool). `next --best` is the headroom-ranked pick instead. **`next` classifies its dead ends and only ONE of them is fixed by `add`** (`cc_account_lib.ts::classifyNextOutcome`, pure + tested — the decision lived in the CLI and was unreachable by the suite until 2026-08-06): empty registry → `add`; **keychain unreadable → say so and never prescribe `add`**, because `security` exits non-zero for a locked keychain and a denied ACL as well as for item-not-found, and `add` would overwrite an intact, non-re-derivable blob with the current login's token; no stored credentials anywhere → re-`add` *while logged in as each*; **active seat unknown → refuse ONLY under `--best`**, which excludes the active seat via `rankProfiles` so a null active excludes nothing and would "switch" onto the seat already in use; cycle mode keeps its documented "start from the beginning" fallback, which is the only route back when `~/.claude.json` has lost its `oauthAccount`; every profile readable-but-incoherent → its own outcome, because `use` refuses those and a deterministic picker would otherwise select the same broken profile forever without ever reaching the healthy ones; only-the-active-seat → **exit 0**, same message and same rc in both modes. **An unreadable profile blocks only when nothing else is usable** — the first cut returned it unconditionally, so one denied ACL hard-failed the whole rotation while four profiles sat switchable. Otherwise skipped profiles (no credentials, unreadable, incoherent) are reported on **stderr** and the switch proceeds. **Unknown flags hard-error on `next`, `reset`, `prune` and `order`** (`--dry-rn`, `-n`, `--bset` all used to parse to "switch for real"; `order` silently *stripped* flags and rewrote the cycle). A help token anywhere in the argv prints usage before any flag parse, so `next --help` is not rejected as unknown. **The Keychain credentials item is GLOBAL and every running `claude` rewrites it on token refresh** — so a live session on account A can clobber the credentials half moments after a switch to B, while `~/.claude.json` still says B; `add` (and the outgoing re-capture) then bottle **A's token under B's identity**. The CLI presents A's token, the server resolves entitlement from it rather than from B's seat, and a team seat with a personal-org token bills as metered API usage — surfacing as **"Credit balance is too low"** on an account with no metered balance, with nothing pointing at the switch. Hit live 2026-08-01: `Net-T3` held a `max` token under an `Evinced RD` (`claude_team`) seat, the only incoherent one of five team profiles. `cc_account_lib.ts::seatIncoherence` compares `credentials.claudeAiOauth.subscriptionType` against `oauthAccount.organizationType` and blocks all three write paths (`add` refuses to store, `use` refuses to switch, the outgoing re-capture skips rather than overwrite a good record). It **fails open** — only a definite contradiction between two known plan types (`claude_team`↔`team`, `claude_max`↔`max`) blocks anything, so an unfamiliar org type or unreadable blob never strands a working profile. Repair = quit every other `claude`, `/login`, pick the org, `add <name>`. **`reset` forgets EVERY stored login and starts over** (added 2026-08-01): it drains the whole `stark-cc-token` Keychain service by looping an **un-named** `security delete-generic-password` rather than walking registry names — that catches **orphans**, records whose registry entry was lost, which nothing can enumerate by name and a name-walk leaves behind invisibly. It **previews by default and acts only under `--yes`** (the inverse of `next`, because an OAuth blob cannot be re-derived — so a mistyped flag degrades to a preview, `--dry-run` beats `--yes`, and unknown flags hard-error). It deliberately does **not** touch the live `Claude Code-credentials` item or `~/.claude.json`: you stay logged in as the current account and can `add` it back immediately, where a reset that logged you out would leave nothing to rebuild from. Usage snapshots are **kept** unless `--snapshots` — they cannot be regenerated retroactively and stay valid for any seat you re-`add`. **`remove <name>` / `prune` forget profiles** (added 2026-07-29; the tool could accumulate profiles but never drop one): `remove` deletes the stored credentials *and* the registry entry and is irreversible — an OAuth blob cannot be re-derived — and warns when the target is the active seat, since `use` re-captures the outgoing account only while it is still registered. `prune` drops every entry with no stored credentials. It **refuses outright when any profile is unreadable** rather than treating "could not read" as "nothing there" — as the two-state probe did, which emptied the entire registry (names, seat keys, the hand-set `order` cycle) on a locked keychain while the Keychain records survived with nothing left to enumerate them by name. Given a readable keychain it destroys nothing recoverable, since an entry qualifies precisely because there is no credential left to destroy. Keep such placeholders if you intend to re-`add` those accounts, because they hold their rotation slot while a pruned one rejoins at the end of the cycle. **`refresh <name>` / `refresh --all` renews a profile without a browser** (added 2026-08-09; `tools/cc_refresh_lib.ts` pure + `cmdRefresh` in the CLI): `POST https://platform.claude.com/v1/oauth/token` with `grant_type=refresh_token` and the CLI's own public client id (both read out of the binary, never guessed) returns `expires_in=28800` (8h access) + `refresh_token_expires_in≈2405096` (~28d refresh). This closed the actual rot: an access token lives 8h, so **every** profile was stale within a day and dead within a month, and the only known repair was `claude /login` × 10. **The refresh token ROTATES — the sent token is dead on arrival — so a refresh whose result is not persisted DESTROYS the profile.** That is the pre-existing bug in one line: `use` restored a snapshot, the CLI refreshed, the rotated token was never written back. Everything about the command follows from rotation: it holds `~/.claude/.cc-refresh.lock` (two concurrent refreshes of one profile both send the same token; the server honours one, the loser stores a dead one), it runs **sequentially**, **`--dry-run` stops before the network call** (a preview that rotated the token it was inspecting is the one unacceptable outcome), and it **never refreshes the active seat** — rotating the live login's token leaves the running CLI to discover it hours later as a forced `/login`. The 200 response carries `account.uuid` + `organization.uuid`, i.e. the seat key, so a profile holding a **different** seat's token is flagged — the only check that catches a swap between two profiles of the SAME plan type, which `seatIncoherence` (plan-string comparison) structurally cannot. Once `refreshTokenExpiresAt` passes, nothing recovers it: `/login` + `add`. Live run 2026-08-09: 7 of 11 profiles renewed from expired (up to 42h stale) with zero logins.
  - **Two same-day corrections, both found by testing the shipped command** (`refresh` v1 = PR #861; fixes = PR #862). **(1) The active seat is RE-CAPTURED, not skipped.** Skipping it looks safe and is how `Team-4` rotted into a `401 revoked` needing a browser login: the live Keychain item is global and the running CLI rotates it on its own schedule, so the stored copy dies while its own `expiresAt` still reads hours ahead and `classifyRefresh` reports `fresh` — a dead profile that every check calls healthy. `activeCopyIsStale` compares the stored refresh token against the live one (a fact, not an inference from timestamps rotation never updates) and copies the live blob into the profile; no network, no rotation, and it only ever overwrites an already-dead credential. Caught live on `Team-3` (stored `448e201b` vs live `ac8674c7`) minutes before it would have gone the same way. **(2) A seat mismatch WARNS AND STILL WRITES.** v1 skipped the write, which discarded the token that had just rotated and killed the profile — the precise outcome the module exists to prevent. The sent token is dead on arrival, so the response is the only live credential for that seat; refusing does not restore the old state, it destroys the current one. The profile had been refreshing that seat's token all along, so the *registry label* drifted, not the credential — `add <name>` re-labels it. General lesson: for a rotating credential, "refuse to write" is never the safe default.
  - **Headless auth also works from env vars alone, and that path never refreshes.** `CLAUDE_CODE_OAUTH_TOKEN` (+ `CLAUDE_CODE_OAUTH_SCOPES`) with an **empty** `HOME` authenticates and returns a normal result — verified end-to-end on refreshed `Max-1` + `Team-0` — while writing **no** credentials, leaving `oauthAccount` absent, and **not touching the global Keychain item**. So a dispatched subprocess can carry its own seat instead of riding the one global login that every parallel session fights over. The constraint: `CLAUDE_CODE_OAUTH_REFRESH_TOKEN` is **inert** — injected with an expired access token the CLI fails `401 OAuth access token has expired` without attempting a refresh (proven twice with the same token; the identical second failure shows it was never consumed). Env injection is therefore only valid **while a seat's access token is unexpired**, which is what `refresh` now guarantees. Wiring it into the four dispatch env-builders (all already funnel through `claude_auth_lib.ts::applyClaudeAuth`) is the follow-up; keep these vars OFF `AGENT_ENV_ALLOWLIST` so an ambient value can never shadow an injected seat.
- `/stark-refactor-plan [target-dir]` — planning-only refactor analysis: inspect any repo and emit `REFACTOR_PLAN.md` + `REFACTOR_BACKLOG.json` (evidence-based, phased, file-by-file). Pure-guidance skill (no TS dispatcher); never modifies source. Exact output templates live in `skill/stark-refactor-plan/references/`. **Playground-scope guard:** the recommendation-emitting subagents (`target-architecture`/`phase-planner`/`artifact-synthesis`) scope-match the target — no production layering / rollback-per-phase ceremony / monitoring-migration phases for a single-user playground repo; the target is the smallest structure that fixes the real findings. The iac review rubrics (`terraform.md`/`terragrunt.md`) carry a parallel **scale-match** note (real infra defects always in scope; enterprise controls — multi-region HA/DR, org policies, mandatory CMEK — only when the config declares that scale).

### Project Setup & Docs

- `/stark-init-docs [--template|--backfill|--upgrade|--clean]` — scaffold dev docs

## Conventions

- **Docs live with the code** under `docs/`, folder per type: `adr/` (decisions — `NNNN-<topic>.md`, MADR-lite, immutable: supersede, don't edit), `specs/` (`YYYY-MM-DD-<topic>-spec.md`), `retros/` (`YYYY-MM-DD-<topic>-retro.md`). **There is no `docs/plans/`.** Since the `/stark-author` two-stage pipeline (2026-08-01) the plan IS part of the spec — the task DAG, per-task done-whens and the closing verification command live inside the spec doc, which `/stark-build` consumes. Never create `docs/plans/` or a `*-plan.md` doc. The existing `docs/plans/` files are historical archive: leave them in place, don't treat them as the pattern to follow. Tier by blast radius: trivial → PR only · feature → spec · architectural → ADR + spec. Guarded by `tools/doc_convention.test.ts`.
- Prompts are per-agent: each LLM gets its own version of each domain
- Domain IDs are slugs derived from filenames: `01-architecture.md` → `architecture`
- Config uses JSON, prompts use markdown
- Agent preambles in `agent.md`, domain prompts in `NN-domain.md`
- **Every skill honors `--help`.** Each `skill/*/SKILL.md` opens with a `## Help` block right after its frontmatter that points at the shared `standards/help.md` protocol: a standalone `--help`/`-h`/`help` token in `$ARGUMENTS` prints the skill's purpose + usage + `## Arguments` and stops (no preflight, no phases, side-effect-free). The `skill_smoke_test.test.ts` guard asserts every skill references `standards/help.md`.

## GitHub Apps

Source of truth is the `APPS` map in `tools/github_app_lib.ts`. All three are the **21S** apps on org `21-Stark-AI`; each is also installed on `GetEvinced` to cover repos still pending migration. The old `GetEvinced`-era apps (app IDs `30667xx`, pre-`_21S` keychain entries) are **retired**.

**They do NOT act as `aryeh-stark`** — an installation token authors as the bot (`app/stark-claude[bot]` etc.). Earlier wording here claimed otherwise and was wrong. Their only sanctioned use is **posting review findings**, where the three separate authors carry the model attribution. Everything else on a PR is `gh`/`aryeh-stark` — see the identity bullet under Operating Principles.

| App (display name) | App ID | Install (21-Stark-AI) | Install (GetEvinced) | Keychain |
|--------------------|--------|-----------------------|----------------------|----------|
| stark-claude (Stark Claude 21S) | 4094779 | 141330560 | 141330785 | STARK_CLAUDE_PRIVATE_KEY_21S |
| stark-codex (Stark Codex 21S) | 4094776 | 141330526 | 141330738 | STARK_CODEX_PRIVATE_KEY_21S |
| stark-gemini (Stark Gemini 21S) | 4094781 | 141330618 | 141330831 | STARK_GEMINI_PRIVATE_KEY_21S |
